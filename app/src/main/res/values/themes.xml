<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleRecipe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="Theme.SimpleRecipe" parent="Base.Theme.SimpleRecipe" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/lightgrey</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/fake_store</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.SimpleRecipe</item>

    </style>

</resources>